# Website Opportunity Flow

Finds businesses with concrete website improvement signals: old copyright text, hosted builders, under-construction pages, or weak web infrastructure. The normal scorer also emits `website_score` so these leads can be prioritized for web-design outreach.

Run:

```bash
pnpm generate:opportunities
```

Requires `SERPER_API_KEY` in `.env`.
