# Apollo-Enriched Dork Flow

Uses a tight founder/agency dork through Serper, crawls discovered company sites, then enriches the resulting leads through Apollo organization enrichment.

Run:

```bash
pnpm generate:apollo
```

Requires `SERPER_API_KEY` and `APOLLO_API_KEY` in `.env`.
