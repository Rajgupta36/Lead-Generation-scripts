# Google Maps Local Flow

Discovers local businesses through `serpapi_maps`, then crawls each business website for contact pages, email, phone, social links, and booking links.

Run:

```bash
pnpm generate:maps
```

Requires `SERPAPI_API_KEY` in `.env`.
